# FE3H battalions

It's a node project, so node is required.

After installing node, run `npm install`. The development server can then be
started using `npm run dev`. To build for production, run `npm run build`.
